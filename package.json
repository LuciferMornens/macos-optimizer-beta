{
  "name": "macos-optimizer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "OS_ACTIVITY_MODE=disable tauri dev --features app",
    "build": "tauri build --features app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
