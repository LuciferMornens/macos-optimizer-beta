{
  "name": "macos-optimizer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --features app,parallel-scan,metrics,metadata-cache,cache-refresh",
    "build": "tauri build --features app,parallel-scan,metrics,metadata-cache,cache-refresh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
